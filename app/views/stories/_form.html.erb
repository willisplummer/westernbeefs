<%= form_for([@article, @article.stories.build]) do |f| %>
	<p>
		<%= f.label :page_count %><br>
		<%= f.integer :page_count %>
	</p>
	<p>
		<%= f.label :title %><br>
		<%= f.string :title %>
	</p>
	<p>
		<%= f.submit %>
	</p>
<% end %>